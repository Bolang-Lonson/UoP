import textio.TextIO;

public class TypeCasting {
    public static void main(String args[]) {

        // Printing the letters of the alphabet
        
        for (char letter = 'A'; letter <= 'z'; letter++) {
            TextIO.putln(letter);
        }
    }
}
